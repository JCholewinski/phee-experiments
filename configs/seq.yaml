training:
  output_dir: outputs/seq
  output_dir_final: outputs/seq/final
  per_device_train_batch_size: 8
  num_train_epochs: 2
  logging_steps: 10
  learning_rate: 5e-5
  save_strategy: epochs

model:
  name: bert-base-cased

data:
  train_path: data/raw/train_main.json
  test_path: data/raw/test_main.json